version: 2

project_name: mimi-remote

builds:
  - id: agentd
    main: ./cmd/agentd
    binary: agentd
    env:
      - CGO_ENABLED=0
      # 发布必须使用 go.mod 声明的工具链，禁止 Go 自动下载并切换到更高版本。
      - GOTOOLCHAIN=local
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{ .Version }}

  - id: linux-tray
    main: ./cmd/mimi-remote-tray
    binary: mimi-remote-tray
    env:
      - CGO_ENABLED=0
      - GOTOOLCHAIN=local
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.releaseVersion={{ .Version }}

# 正式 tag 必须显式设置 MIMI_MACOS_SIGNING=required。GoReleaser 会先使用
# Developer ID 对两个 Darwin 二进制签名并完成 Apple notarization，再把签名后的
# 二进制交给 archive/checksum/Homebrew Formula 阶段。普通 snapshot 不接触发布私钥。
notarize:
  macos:
    - enabled: '{{ eq (envOrDefault "MIMI_MACOS_SIGNING" "disabled") "required" }}'
      ids:
        - agentd
      sign:
        certificate: "{{ .Env.MACOS_SIGN_P12 }}"
        password: "{{ .Env.MACOS_SIGN_PASSWORD }}"
      notarize:
        issuer_id: "{{ .Env.MACOS_NOTARY_ISSUER_ID }}"
        key_id: "{{ .Env.MACOS_NOTARY_KEY_ID }}"
        key: "{{ .Env.MACOS_NOTARY_KEY }}"
        wait: true
        timeout: 20m

archives:
  - id: agentd
    ids:
      - agentd
      - linux-tray
    allow_different_binary_count: true
    formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    files:
      - README.md
      - LICENSE
      - NOTICE.md
      - TRADEMARKS.md
      - THIRD_PARTY_NOTICES.md
      - SECURITY.md
      - config.example.json
      - packaging/systemd/mimi-remote.service
      - scripts/install-linux.sh
      - scripts/install-linux-tray.sh
      - packaging/linux/mimi-remote.desktop
      - cmd/mimi-remote-tray/assets/mimi.png
      - cmd/mimi-remote-tray/assets/mimi-remote-symbolic.svg
      - cmd/mimi-remote-tray/assets/mimi-remote-attention-symbolic.svg
      - cmd/mimi-remote-tray/assets/mimi-remote-offline-symbolic.svg

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

# 显式固定完整源码与 Release 仓库；历史后端快照导出链路已退役，
# snapshot Formula 不再生成指向第二仓库的下载地址。
release:
  github:
    owner: gaixianggeng
    name: mimi-remote
  # GitHub Release 已创建但 tap 推送失败时，允许同一 tag 的 workflow 重跑。
  # keep-existing 保留既有发布说明，只替换由该 tag 重新构建的同名附件。
  mode: keep-existing
  replace_existing_artifacts: true

brews:
  - name: mimi-remote
    ids:
      - agentd
    repository:
      owner: gaixianggeng
      name: homebrew-tap
      git:
        url: git@github.com:gaixianggeng/homebrew-tap.git
        private_key: "{{ .Env.TAP_DEPLOY_KEY }}"
    directory: Formula
    homepage: https://github.com/gaixianggeng/mimi-remote
    description: iPad companion agent for user-owned Mac developer environments
    install: |
      bin.install "agentd"
    test: |
      system "#{bin}/agentd", "version"
    custom_block: |
      service do
        run [opt_bin/"agentd", "serve"]
        environment_variables PATH: std_service_path_env
        keep_alive true
        log_path var/"log/mimi-remote.log"
        error_log_path var/"log/mimi-remote.log"
      end
    caveats: |
      首次使用：
        codex --version
        codex app-server --help
        agentd up

      Mimi Remote App 优先扫描 agentd up 输出的二维码；二维码过期后运行 agentd pair 刷新。
      排障：
        agentd status
        agentd doctor --fix
        agentd logs
